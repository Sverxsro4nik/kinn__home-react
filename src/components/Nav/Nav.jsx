import React from "react";

function Nav(){
    return(
        <nav className="header-nav">
            <a href="#">shop</a>
            <a href="#">style quiz</a>
            <a href="#">about us</a>
            <a href="#">stories</a>
        </nav>
    );
}

export default Nav;